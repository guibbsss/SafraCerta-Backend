-- Banco usado em application.yml (jdbc:mysql://localhost:3306/safracerta)
-- Execute este arquivo primeiro (como usuário com permissão para criar schema).

CREATE DATABASE IF NOT EXISTS safracerta
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
